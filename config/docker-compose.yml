services:
    web:
      container_name: researcher-router-bot
      build:
        context: ..
        dockerfile: config/Dockerfile
      env_file:
        - ../.env
      restart: unless-stopped
    # Qdrant - commented out, using cloud instance
    # Uncomment below for local development
    # qdrant:
    #   image: qdrant/qdrant
    #   ports:
    #     - "6333:6333"
    #     - "6334:6334"
    #   volumes:
    #     - ./qdrant_storage:/qdrant/storage
    #   restart: unless-stopped   